# 09 — Wait/intersect at radius 7

## Hypothesis

Radius 7 may provide most of the exact-tail benefit with a substantially smaller lookup structure than radius 8.

## Intervention

The algorithm and exactness contract match method 08, but the complete backward ball ends at radius 7. The separate notebook makes runtime, memory, solve rate, and path-length differences directly visible.

